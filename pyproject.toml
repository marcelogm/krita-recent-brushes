[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov --cov-report=term-missing"

[tool.coverage.run]
source = ["pykrita/recent_brushes"]

[tool.coverage.report]
fail_under = 95

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
